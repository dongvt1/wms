<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.modules.planning.mapper.ItemMasterMapper">

    <select id="searchByMpn" resultType="com.cy.modules.planning.entity.ItemMaster">
        SELECT * FROM pl_item_master WHERE mpn LIKE CONCAT('%', #{mpn}, '%') ORDER BY create_time DESC
    </select>

    <select id="searchByIpn" resultType="com.cy.modules.planning.entity.ItemMaster">
        SELECT * FROM pl_item_master WHERE ipn LIKE CONCAT('%', #{ipn}, '%') ORDER BY create_time DESC
    </select>

    <select id="selectByLifecycle" resultType="com.cy.modules.planning.entity.ItemMaster">
        SELECT * FROM pl_item_master WHERE lifecycle_status = #{lifecycleStatus} ORDER BY category, ipn
    </select>

    <select id="selectByCategory" resultType="com.cy.modules.planning.entity.ItemMaster">
        SELECT * FROM pl_item_master WHERE category = #{category} ORDER BY ipn
    </select>

</mapper>
