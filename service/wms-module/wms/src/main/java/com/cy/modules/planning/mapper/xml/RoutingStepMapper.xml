<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.modules.planning.mapper.RoutingStepMapper">

    <select id="selectByRoutingId" resultType="com.cy.modules.planning.entity.RoutingStep">
        SELECT rs.*, wc.center_name AS work_center_name, wc.center_code AS work_center_code
        FROM pl_routing_step rs
        LEFT JOIN pl_work_center wc ON wc.id = rs.work_center_id
        WHERE rs.routing_id = #{routingId}
        ORDER BY rs.step_order ASC
    </select>

    <delete id="deleteByRoutingId">
        DELETE FROM pl_routing_step WHERE routing_id = #{routingId}
    </delete>

</mapper>
