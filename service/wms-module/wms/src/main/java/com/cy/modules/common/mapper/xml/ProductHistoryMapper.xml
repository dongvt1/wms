<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.modules.common.mapper.ProductHistoryMapper">

    <!-- Get product history by product ID -->
    <select id="selectByProductId" resultType="com.cy.modules.common.entity.ProductHistory">
        SELECT * FROM product_history WHERE product_id = #{productId} ORDER BY create_time DESC
    </select>

    <!-- Get product history by user ID -->
    <select id="selectByUserId" resultType="com.cy.modules.common.entity.ProductHistory">
        SELECT * FROM product_history WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <!-- Get product history by action -->
    <select id="selectByAction" resultType="com.cy.modules.common.entity.ProductHistory">
        SELECT * FROM product_history WHERE action = #{action} ORDER BY create_time DESC
    </select>

</mapper>