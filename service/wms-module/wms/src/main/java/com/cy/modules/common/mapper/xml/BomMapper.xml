<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.modules.common.mapper.BomMapper">

    <select id="selectByProductId" resultType="com.cy.modules.common.entity.Bom">
        SELECT b.*, p.name AS product_name
        FROM pl_bom b
        LEFT JOIN product p ON p.id = b.product_id
        WHERE b.product_id = #{productId}
        ORDER BY b.is_default DESC, b.create_time DESC
    </select>

    <select id="selectByStatus" resultType="com.cy.modules.common.entity.Bom">
        SELECT b.*, p.name AS product_name
        FROM pl_bom b
        LEFT JOIN product p ON p.id = b.product_id
        WHERE b.status = #{status}
        ORDER BY b.create_time DESC
    </select>

</mapper>
