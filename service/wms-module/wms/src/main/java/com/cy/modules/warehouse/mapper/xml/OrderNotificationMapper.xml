<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.modules.warehouse.mapper.OrderNotificationMapper">

    <!-- Result Map -->
    <resultMap id="OrderNotificationResultMap" type="com.cy.modules.warehouse.entity.OrderNotification">
        <id     column="id"            jdbcType="VARCHAR"   property="id" />
        <result column="create_by"     jdbcType="VARCHAR"   property="createBy" />
        <result column="create_time"   jdbcType="TIMESTAMP" property="createTime" />
        <result column="update_by"     jdbcType="VARCHAR"   property="updateBy" />
        <result column="update_time"   jdbcType="TIMESTAMP" property="updateTime" />
        <result column="sys_org_code"  jdbcType="VARCHAR"   property="sysOrgCode" />
        <result column="order_id"      jdbcType="VARCHAR"   property="orderId" />
        <result column="type"          jdbcType="VARCHAR"   property="type" />
        <result column="recipient"     jdbcType="VARCHAR"   property="recipient" />
        <result column="subject"       jdbcType="VARCHAR"   property="subject" />
        <result column="content"       jdbcType="VARCHAR"   property="content" />
        <result column="status"        jdbcType="VARCHAR"   property="status" />
        <result column="sent_at"       jdbcType="TIMESTAMP" property="sentAt" />
        <result column="error_message" jdbcType="VARCHAR"   property="errorMessage" />
        <result column="retry_count"   jdbcType="INTEGER"   property="retryCount" />
    </resultMap>

    <!-- Get list of order notifications -->
    <select id="getNotificationsByOrderId" parameterType="java.lang.String" resultMap="OrderNotificationResultMap">
        SELECT 
            n.id,
            n.order_id,
            n.type,
            n.recipient,
            n.subject,
            n.content,
            n.status,
            n.sent_at,
            n.error_message,
            n.retry_count,
            n.create_time
        FROM order_notifications n
        WHERE n.order_id = #{orderId}
        ORDER BY n.create_time DESC
    </select>

    <!-- Get list of pending notifications -->
    <select id="getPendingNotifications" resultMap="OrderNotificationResultMap">
        SELECT 
            id,
            order_id,
            type,
            recipient,
            subject,
            content,
            status,
            sent_at,
            error_message,
            retry_count,
            create_time
        FROM order_notifications
        WHERE status = 'PENDING'
        ORDER BY create_time ASC
        LIMIT 100
    </select>

    <!-- Get list of failed notifications -->
    <select id="getFailedNotifications" parameterType="java.lang.Integer" resultMap="OrderNotificationResultMap">
        SELECT 
            id,
            order_id,
            type,
            recipient,
            subject,
            content,
            status,
            sent_at,
            error_message,
            retry_count,
            create_time
        FROM order_notifications
        WHERE status = 'FAILED' AND retry_count &lt; #{maxRetryCount}
        ORDER BY create_time ASC
        LIMIT 50
    </select>

</mapper>