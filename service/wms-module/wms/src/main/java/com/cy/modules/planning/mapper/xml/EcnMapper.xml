<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.modules.planning.mapper.EcnMapper">

    <select id="selectByBomId" resultType="com.cy.modules.planning.entity.Ecn">
        SELECT * FROM wh_ecn WHERE bom_id = #{bomId} ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" resultType="com.cy.modules.planning.entity.Ecn">
        SELECT * FROM wh_ecn WHERE status = #{status} ORDER BY create_time DESC
    </select>

</mapper>
