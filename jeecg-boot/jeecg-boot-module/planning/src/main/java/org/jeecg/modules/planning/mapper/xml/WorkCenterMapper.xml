<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.planning.mapper.WorkCenterMapper">

    <select id="selectByStatus" resultType="org.jeecg.modules.planning.entity.WorkCenter">
        SELECT * FROM wh_work_center WHERE status = #{status} ORDER BY create_time DESC
    </select>

    <select id="selectByProductionLineId" resultType="org.jeecg.modules.planning.entity.WorkCenter">
        SELECT * FROM wh_work_center WHERE production_line_id = #{productionLineId} ORDER BY center_code ASC
    </select>

</mapper>
