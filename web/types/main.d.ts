// Application parameters
export type MainAppProps = {
  container?: HTMLElement;
  // Hide sidebar（menu）
  hideSider?: boolean;
  // hide top
  hideHeader?: boolean;
  // hide manyTab switch
  hideMultiTabs?: boolean;
}
